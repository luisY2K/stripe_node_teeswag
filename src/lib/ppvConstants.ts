/** Must match the Billing meter `event_name` and meter event `event_name`. */
export const PPV_METER_EVENT_NAME = "ppv_view";

/** Lookup key for the metered PPV price (EUR per view, monthly period). */
export const PPV_PRICE_LOOKUP_KEY = "awesome_ppv_view_eur";
